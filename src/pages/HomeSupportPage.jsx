import PillarPageLayout from '../components/layout/PillarPageLayout';
import { PILLAR_PAGES } from '../utils/siteContent';

export default function HomeSupportPage() {
  return <PillarPageLayout data={PILLAR_PAGES.home_support} />;
}
